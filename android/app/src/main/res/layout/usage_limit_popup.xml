<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="#AA000000"> <!-- Fondo transparente -->

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="20dp"
        android:background="@drawable/rounded_rectangle_bg"
        android:gravity="center">

        <TextView
            android:id="@+id/usage_limit_message"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tiempo de uso excedido"
            android:textSize="18sp"
            android:textColor="@android:color/white"
            android:layout_marginBottom="10dp" />

        <!-- Botón para 1 minuto más -->
        <Button
            android:id="@+id/one_more_minute_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="1 Minuto Más"
            android:textSize="16sp"
            android:layout_marginBottom="10dp"
            android:background="@drawable/rounded_button_bg"
            android:textColor="@android:color/white" />

        <!-- Botón para cerrar -->
        <Button
            android:id="@+id/close_popup_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Cerrar"
            android:textSize="16sp"
            android:background="@drawable/rounded_button_bg"
            android:textColor="@android:color/white" />
    </LinearLayout>
</LinearLayout>
